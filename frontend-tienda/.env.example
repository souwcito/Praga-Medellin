# URL base de la API REST (Laravel). Ej: https://api.pragamedellin.co/api
VITE_API_URL=

# true = usa datos simulados (services/mockApi.js). false = consume el backend real.
VITE_USE_MOCK=true

# Llave pública de Wompi (checkout). Modo prueba: pub_test_... / producción: pub_prod_...
VITE_WOMPI_PUBLIC_KEY=